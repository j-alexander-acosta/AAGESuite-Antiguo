from django.urls import path
from gestion import views

app_name = 'gestion'

urlpatterns = [
    # path(
    #     r'^$',
    #     views.HomeTemplateView.as_view(),
    #     name='home'
    # ),
]
